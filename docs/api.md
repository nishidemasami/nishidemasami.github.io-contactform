# API

## 現状

このリポジトリには、2026-05-10 時点で `/api/` ディレクトリが存在しません。そのため、HTTP エンドポイント、ハンドラー、リクエスト/レスポンス形式などの API 実装ソースは未収録です。

このページは空欄ではなく、**API 実装がまだ追加されていない** という現在の状態を明示するための記録です。

## 既存基盤から分かる前提

API 実装は未配置ですが、周辺基盤から以下の前提が読み取れます。

| 観点 | 現在の前提 | 参照 |
| --- | --- | --- |
| 認証 | Cognito User Pool / App Client を使う構成です。JWT Issuer も CloudFormation Output で公開されます。 | [認証](auth.md) |
| データ永続化 | 問い合わせは `inquiries` テーブル、ユーザー情報は `users` テーブルに保存する前提です。 | [データベース](db.md) |
| 参照アクセス | `get_inquiries_by_email` 関数と `selectview` ロールが定義されています。 | [データベース](db.md) |
| ORM | DB スキーマから SeaORM エンティティを自動生成するワークフローがあります。 | [CI/CD](cicd.md) |

## API 実装追加時の更新ポイント

`/api/` 配下にソースが追加されたら、少なくとも次をこのページへ反映します。

1. エンドポイント一覧
2. 認証要件と JWT 検証方法
3. 使用テーブル・関数・ロール
4. OpenAPI やリクエスト/レスポンス仕様へのリンク
5. デプロイ方法と CI/CD 連携

## 関連ページ

- [認証](auth.md)
- [データベース](db.md)
- [CI/CD](cicd.md)
